<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Artifisi Com" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Artifisi Com">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <script src="https://cdn.tailwindcss.com"></script>
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :actiontext, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= ["bg-slate-100", (content_for?(:body_class) ? yield(:body_class) : nil)].compact.join(" ") %>">
    <% if notice || alert %>
      <div id="toast-region"
           class="pointer-events-none fixed inset-0 z-50 flex items-end justify-end px-4 pb-4 sm:px-6 sm:pb-6">
        <div class="flex w-full max-w-sm flex-col gap-3"
             role="region" aria-label="Notifications" aria-live="assertive">
          <% if notice %>
            <div class="pointer-events-auto rounded-2xl liquid-glass shadow-lg shadow-indigo-900/10"
                 data-controller="alert"
                 data-alert-dismiss-after-value="5000"
                 data-tone="emerald"
                 role="status" aria-live="polite">
              <div class="flex items-start gap-3 p-4">
                <span class="inline-flex h-9 w-9 items-center justify-center rounded-xl bg-emerald-500/15 text-emerald-500">
                  <svg class="h-5 w-5" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 11 8 14.5 15.5 7" />
                  </svg>
                </span>
                <div class="min-w-0">
                  <p class="text-sm font-semibold text-slate-900">Success</p>
                  <p class="mt-1 text-sm text-slate-600"><%= notice %></p>
                </div>
                <button class="ml-auto inline-flex size-7 items-center justify-center rounded-full text-slate-300 transition hover:text-slate-500"
                        data-action="alert#close">
                  <span class="sr-only">Close</span>
                  <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m6 6 8 8M14 6l-8 8" />
                  </svg>
                </button>
              </div>
            </div>
          <% end %>

          <% if alert %>
            <div class="pointer-events-auto rounded-2xl liquid-glass shadow-lg shadow-rose-900/10"
                 data-controller="alert"
                 data-alert-dismiss-after-value="7000"
                 data-tone="rose"
                 role="alertdialog" aria-live="assertive">
              <div class="flex items-start gap-3 p-4">
                <span class="inline-flex h-9 w-9 items-center justify-center rounded-xl bg-rose-500/15 text-rose-500">
                  <svg class="h-5 w-5" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M10 6v5m0 3h.01" />
                    <circle cx="10" cy="10" r="7" />
                  </svg>
                </span>
                <div class="min-w-0">
                  <p class="text-sm font-semibold text-slate-900">Heads up</p>
                  <p class="mt-1 text-sm text-slate-600"><%= alert %></p>
                </div>
                <button class="ml-auto inline-flex size-7 items-center justify-center rounded-full text-slate-300 transition hover:text-slate-500"
                        data-action="alert#close">
                  <span class="sr-only">Close</span>
                  <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="m6 6 8 8M14 6l-8 8" />
                  </svg>
                </button>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
    <main class="<%= content_for?(:main_class) ? yield(:main_class) : "pt-20" %>">
      <%= yield %>
    </main>
  </body>
</html>
