# Copy this file to `.env.docker` and fill in the values before running Docker Compose.

# Rails master key used to decrypt credentials. Required for boot.
RAILS_MASTER_KEY=

# Optionally override Rails log level (defaults to info in production).
#RAILS_LOG_LEVEL=debug
